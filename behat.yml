default:

  extensions:
    KrzysztofRewak\Larahat\BehatExtension:
      env: ".env.behat"

  suites:
    http:
      contexts:
        - Brewmap\Testing\InternalHttpRequesting
      paths:
        - features/http

    eloquent:
      contexts:
        - Brewmap\Testing\Eloquent\Users
      paths:
        - features/eloquent
